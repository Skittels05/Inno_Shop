# Инструкция по настройке перед запуском Docker

## ✅ Что уже настроено

В `docker-compose.yml` уже настроены:
- ✅ SQL Server для Users и Products (базы данных)
- ✅ RabbitMQ (очередь сообщений)
- ✅ JWT настройки
- ✅ Строки подключения к базам данных
- ✅ Порты и зависимости между сервисами

## ⚠️ Что нужно настроить ПЕРЕД запуском

### 1. SMTP настройки для отправки email (ОБЯЗАТЕЛЬНО для Users API)

В `docker-compose.yml` в секции `users-api` нужно заменить следующие значения на ваши реальные:

```yaml
- SmtpSettings__Server=smtp.gmail.com          # Ваш SMTP сервер
- SmtpSettings__Port=587                       # Порт SMTP (обычно 587 для TLS)
- SmtpSettings__SenderName=Inno Shop           # Имя отправителя
- SmtpSettings__SenderEmail=your-email@gmail.com  # Email отправителя
- SmtpSettings__Username=your-email@gmail.com     # Логин SMTP
- SmtpSettings__Password=your-app-password        # Пароль приложения (не обычный пароль!)
```

**Важно для Gmail:**
- Используйте **пароль приложения** (App Password), а не обычный пароль
- Создайте пароль приложения: https://myaccount.google.com/apppasswords
- Включите двухфакторную аутентификацию в Google аккаунте

**Для других почтовых сервисов:**
- **Outlook/Hotmail**: `smtp-mail.outlook.com`, порт 587
- **Yahoo**: `smtp.mail.yahoo.com`, порт 587
- **Другой SMTP**: уточните настройки у вашего провайдера

### 2. Проверка портов

Убедитесь, что следующие порты свободны:
- `1433` - SQL Server для Users
- `1434` - SQL Server для Products
- `5672` - RabbitMQ
- `15672` - RabbitMQ Management UI
- `5291` - Users API
- `5292` - Products API

### 3. Docker и Docker Compose

Убедитесь, что установлены:
- Docker Desktop (Windows/Mac) или Docker Engine (Linux)
- Docker Compose (обычно входит в Docker Desktop)

## 🚀 Запуск

После настройки SMTP выполните:

```bash
docker-compose up -d
```

Или для просмотра логов:

```bash
docker-compose up
```

## 📝 Проверка работы

1. **Базы данных**: Проверьте логи контейнеров `users-db` и `products-db`
2. **RabbitMQ**: Откройте http://localhost:15672 (guest/guest)
3. **Users API**: http://localhost:5291/swagger
4. **Products API**: http://localhost:5292/swagger

## ⚠️ Важные замечания

1. **Пароль базы данных**: В `docker-compose.yml` используется `YourStrong@Passw0rd` - для продакшена измените на более сложный пароль
2. **JWT Secret Key**: Для продакшена сгенерируйте новый секретный ключ
3. **SMTP**: Без правильных SMTP настроек функции подтверждения email и восстановления пароля не будут работать

## 🔧 Альтернатива: использование .env файла

Вы можете создать `.env` файл для хранения чувствительных данных:

```env
SMTP_SERVER=smtp.gmail.com
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password
DB_PASSWORD=YourStrong@Passw0rd
JWT_SECRET_KEY=Y2F0Y2hfdGhpc19pZl9jb3VfbG9sX3NlY3JldF9rZXlfaXNfYXQgbGVhc3QgMzIgY2hhcnMhISE=
```

И использовать в `docker-compose.yml`:
```yaml
- SmtpSettings__Password=${SMTP_PASSWORD}
```

**Не забудьте добавить `.env` в `.gitignore`!**


